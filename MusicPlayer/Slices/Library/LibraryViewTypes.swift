// MARK: - View Selection
enum LibraryViewMode: String, CaseIterable {
    case artists = "Artists"
    case albums = "Albums"
    case songs = "Songs"
}

// MARK: - Display Mode
enum DisplayMode {
    case grid
    case list
}
